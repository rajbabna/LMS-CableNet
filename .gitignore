# Secrets — never commit real credentials
.env
.env.*
!.env.example

# Dependencies / build
node_modules/
*.log

# Playwright test artifacts (never commit run output)
test-results/
playwright-report/
blob-report/
playwright/.cache/

# OS junk
Thumbs.db
Desktop.ini
.DS_Store

# Editor
.vscode/
.idea/
*.code-workspace

# Tooling
.claude/

# Not published / not needed in the repo
# (migrations + screenshots + legacy pages are intentionally excluded
#  from the public repo; still on disk and in git history)
sql/
old/
scrrencap/

# Internal docs that contain real account emails / beta-test details
docs/content/separate-account-checklist.md
docs/content/beta-test-kit.md
docs/content/overall-achievements.md

# Project-state docs scrubbed of account emails, kept local only
docs/ACHIEVEMENTS.md
docs/RESUME.md
docs/START-HERE.md
docs/SESSION-HANDOFF.md

# Lesson content source (markdown bundle) — generated HTML lives in the
# repo; the source MD that tools/build-lessons.js reads from stays local
docs/cablenet-courses-bundle/

screencap/
